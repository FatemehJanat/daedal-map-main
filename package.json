{
  "name": "daedalmap-runtime",
  "version": "2.0.0",
  "private": true,
  "description": "Legacy package metadata for the DaedalMap open runtime repository.",
  "scripts": {
    "start": "python -m uvicorn app:app --host 0.0.0.0 --port 7000",
    "dev:server": "python -m uvicorn app:app --host 0.0.0.0 --port 7000 --reload",
    "x402:test:dataset": "node examples/x402_query_dataset_test.mjs"
  },
  "dependencies": {
    "@x402/core": "^2.9.0",
    "@x402/evm": "^2.9.0",
    "@x402/fetch": "^2.9.0",
    "viem": "^2.47.10"
  }
}
