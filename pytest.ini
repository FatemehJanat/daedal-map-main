[pytest]
testpaths = tests
markers =
    spine_gap: known failure tied to the in-flight geometry/loc_id spine rebuild. Re-check after spine work lands; see docs note in the marker reason.
    fixture_drift: known failure where the test fixture no longer matches production behavior or local data. Not a spine issue.
